import { StyleSheet } from 'react-native';

export const textInputStyles = StyleSheet.create({
    textInputStyle: {
        height: 50,
        width: '90%',
        borderWidth: 2,
        borderStyle: 'solid',
        borderRadius: 10,
        fontSize: 18,
        color: 'black',
        fontWeight: 'bold',
        padding: 10
    }

});
